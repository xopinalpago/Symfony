{% extends '@E01/base.html.twig' %}
{% block content %}
<h1>Liste des articles</h1>

<ul>
    {% for article in articles %}
        <li>
            <a href="{{ path('article_show', { 'slug': article.slug }) }}">{{ article.title }}</a>
        </li>
    {% endfor %}
</ul>
{% endblock %}